// Create a string variable with quoted text in it.
// For example: 'How you doin'?', Joey said'.
taskName = "Quoted Text";

function Main(bufferElement) {

    var quotedText = "'How you doin\'?', Joey said.";
        
    WriteLine("Quoted Text: " + quotedText);
}